Hello Margarida,

Following up on your request, we re-ran every payment method directly against `https://clientes.eupago.pt` from the production domain (no tunnel) and captured the exact payloads/headers the adapter now uses. The full report lives in `eupago-support-log.txt`; it starts with the reminder that credit card processing is still pending approval for this merchant, then documents the environment snapshot, both payload formats we attempted (dot and comma decimals), and the raw HTTP bodies that came back from EuPago. We also generated ready-to-run curl commands in `eupago-curl-examples.txt` so your team can reproduce everything with the same API key (`e9c2-89a9-20c2-39c9-2a6b`).

For MB WAY we logged two variants: `value:"39.95"` and `value:"39,95"`. In both cases the API responds with `CUSTOMERPHONE_MISSING` even though the request always includes the `alias` field (and, in the direct curl variant, an explicit phone number). Multibanco behaves similarly regardless of decimal separator—both requests return `{"estado":-10,"resposta":"Os dados inseridos não são válidos"}`, which typically indicates that the entity/sub-entity is not linked to this API key. Credit card requests still return `AMOUNT_MISSING`, which matches your note about the service awaiting approval.

The log ends with five explicit questions for the EuPago support team: (a) confirm MB WAY and Multibanco activation for this key, (b) confirm the required amount field/decimal format, (c) validate the Multibanco entity/sub-entity mapping, (d) tell us whether palmanhac-shop.pt must be whitelisted, and (e) provide a working curl example for each channel. Once those points are resolved on your side we can retest immediately—the adapter is already emitting the payloads you asked for, and the structured logs (`logs/payments.log`) confirm the new headers are being sent with every request.
